#include "main.h"

/**
 * _isupper - Confirm upper case
 * @x: The number to review
 * Return: 1 uppercase or 0 for anything else
 * /

int _isupper(int x)

{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
